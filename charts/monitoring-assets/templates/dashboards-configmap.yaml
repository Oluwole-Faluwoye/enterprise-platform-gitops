{{- range $path, $_ := .Files.Glob "dashboards/**/*.json" }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-{{ base $path | trimSuffix ".json" | lower | replace "_" "-" | replace "." "-" }}
  namespace: monitoring
  labels:
    grafana_dashboard: "1"

data:
  {{ base $path }}: |
{{ $.Files.Get $path | indent 4 }}
{{- end }}