apiVersion: v1
kind: ConfigMap
metadata:
  name: enterprise-platform-dashboards
  namespace: monitoring

  labels:
    grafana_dashboard: "1"

data:
{{ (.Files.Glob "dashboards/*.json").AsConfig | indent 2 }}