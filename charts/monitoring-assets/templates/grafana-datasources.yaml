apiVersion: v1
kind: ConfigMap

metadata:
  name: grafana-datasource-datasources
  namespace: monitoring

  labels:
    grafana_datasource: "1"

data:
  enterprise-datasources.yaml: |
{{ .Files.Get "grafana/datasources/datasources.yaml" | indent 4 }}