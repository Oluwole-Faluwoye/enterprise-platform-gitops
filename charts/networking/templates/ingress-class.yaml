apiVersion: networking.k8s.io/v1

kind: IngressClass

metadata:

  name: {{ .Values.alb.ingressClass }}

  labels:

    {{- include "networking.labels" . | nindent 4 }}

spec:

  controller: ingress.k8s.aws/alb